meta {
  name: Ping
  type: http
  seq: 2
}

get {
  url: {{ihtp_stubs_url}}/ping/ping
  body: none
  auth: none
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
